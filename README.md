# BANK-MANAGEMENT-SYSTEM-C
This is a simple console-based banking application written in C that allows the creation and management of basic customer accounts. The program stores customer information (like name, DOB, address, account type, and deposit amount) in a text file (record.txt), making it easy to view and manage bank records persistently.
